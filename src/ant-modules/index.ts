import { Anchor, Button, Checkbox, Form, Input, Layout, notification, Spin, Table, Tooltip } from 'antd';
export { Anchor, Button, Checkbox, Form, Input, Layout, notification, Spin, Table, Tooltip };

export const { Header, Content, Footer, Sider } = Layout;
export const { Link } = Anchor;
