// import { Anchor, Col, Form, Layout, notification, Row, Table, Tooltip } from 'antd';
export { Anchor, Col, Form, Layout, Row, Table, Tooltip, notification };

export const { Header, Content, Footer, Sider } = Layout;
export const { Link } = Anchor;
